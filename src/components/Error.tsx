import React from 'react';
import { Typography } from '@mui/material';
import { PositionFixedCenter } from '../assets/styles/common.styles';

export const Error = () => {
    return (
        <PositionFixedCenter>
            <Typography variant="h1">Error</Typography>
        </PositionFixedCenter>
    );
};
